# 本轮优化记录

本轮保留 uni-app、Vue 3、Pinia 和现有复习规则，未引入新依赖，也未迁移用户数据。

## 修复

- 编辑卡片正文时保留原小节 ID 与标题，避免卡片意外退出原来的学习小节；跨章节移动和显式清除仍按原规则处理。
- 知识连接说明可以编辑，保存时保留用户内容；确认同一个科目或前置知识不会清空说明。
- 科目详情、卡片编辑、复习初始化失败时提供重试，避免显示空表单或误报学习完成。
- 评分、撤销、删除、备份和编辑操作共用互斥执行与错误提示，卡片保存完成至返回期间保持禁用，防止重复提交。
- 深层知识路径采用迭代排序，支持 20,000 节点测试；请求零层上下文时正确返回空数组。
- 选择器绑定当前值；已掌握且暂停的卡片可以恢复启用。
- 返回科目页时保留滚动组件实例，避免刷新与页面缓存激活同时发生导致 `scrollLeft` 空引用。

## 界面

- 首页突出学习入口；桌面端并排展示学习卡与今日统计，手机端保持单列。
- 知识库默认显示科目，按需展开新建/编辑表单。
- 卡片支持章节、学习状态和全文搜索组合筛选；显示剩余卡片数，按每次 20 张逐步显示。
- 统一加载反馈、空结果提示、长文本换行和按钮禁用状态。

## 性能与维护

- 搜索文本随数据更新预先生成，查询复用索引；页面和服务共用搜索字段定义。
- 首页先按科目分组卡片和状态，避免为每个科目反复扫描全库。
- 薄弱卡计算仅维护每张活跃卡最近 10 次评分，兼容乱序日志及相同时间戳。
- 预置数据已完整时不再重写版本标记，只有迁移/恢复时读取进度与历史日志。
- 学习范围切换直接使用已加载数据计算队列数量；返回页面时刷新数据。
- 抽出公共异步操作和加载组件；补充 GitHub Actions，执行与本地相同的 `npm run check`。

一次本机合成基准：10,000 张卡、每张约 500 字答案、5 个查询，7 轮耗时中位数从 55.68 ms 降至 7.09 ms；首次建立索引另需 22.11 ms。该结果仅衡量搜索计算，不代表整个应用或手机设备的速度。

## 验证范围

- 自动化回归覆盖元数据保留、显式清除、长知识路径、搜索一致性、操作互斥、错误后重试、乱序统计与无冗余存储读写。
- 类型检查与 90 项自动化测试通过（本轮新增 11 项）；H5、微信小程序构建通过。GitHub Actions 配置已添加，尚未在远程运行。
- 浏览器验证桌面与手机布局、搜索/状态筛选、编辑保存后重新打开、进入小节、显示答案、评分及撤销。
- 构建覆盖 H5 和微信小程序；微信真机、输入法及系统存储配额仍需设备验证。
- 存储仍按完整数组读写，局域网多设备冲突处理未在本轮重构。
