# 苦作舟：功能、架构与改进评审说明

> 文档用途：提供给不同大模型进行独立产品与技术评审。
>
> 快照日期：2026-08-24
>
> 评审对象：当前本地工作区（包含尚未提交的可靠性与微信构建调整）
>
> Git 基线：`972c57e Improve review workflow and preset knowledge`

## 1. 项目定位

“苦作舟”是一款面向个人的轻量知识卡片与间隔复习工具。它尝试把传统问答卡片、FSRS 间隔重复和“前置知识路径”结合起来，让用户不仅记住孤立答案，也能在学习新知识时先理解其上下文和依赖关系。

产品当前支持：

- H5 网页端；
- 微信小程序端；
- 完全本地使用，不需要登录；
- JSON 手动备份和跨端迁移；
- 默认附带一套可直接体验的知识库。

当前不是云端知识库、团队协作平台、在线课程系统或社交学习产品。

## 2. 技术栈与运行平台

| 层级 | 当前实现 |
|---|---|
| UI 框架 | uni-app、Vue 3、TypeScript |
| 状态管理 | Pinia |
| 构建工具 | Vite、DCloud uni-app CLI |
| 记忆调度 | `ts-fsrs` 5.4.1 |
| 网页平台 | H5 |
| 移动平台 | 微信小程序 |
| 数据存储 | uni 本地存储；H5 开发环境可使用本地 JSON 共享服务 |
| 自动化测试 | Node Test Runner + tsx |

微信小程序 AppID 已配置，生产构建启用了 URL 校验、Sitemap 校验、ES6、PostCSS 和代码压缩。

## 3. 用户可见功能

### 3.1 首页与每日入口

- 显示今日待复习数量；
- 优先恢复当天未完成的复习会话；
- 展示今日已复习、新学习、重来次数和连续学习天数；
- 展示最近更新的知识库及卡片数量；
- 提供“今日复习”和“路径学习”两个入口；
- 空知识库、加载中和今日完成均有独立状态提示。

### 3.2 知识库管理

支持三级内容组织：

```text
科目
└── 章节
    └── 知识卡
```

科目功能：

- 新建、编辑、删除科目；
- 科目描述；
- 显示卡片数量和最近更新时间；
- 删除科目时级联清理所属章节、卡片、复习状态和复习日志。

章节功能：

- 新建、重命名和删除章节；
- 按章节筛选知识卡；
- 删除章节时保留知识卡，并将其调整为“未分类”。

知识卡功能：

- 新建、编辑、删除；
- 问题与标准答案；
- 备注；
- 标签；
- 重要程度：一般、重要、非常重要；
- 活跃/暂停状态；
- 按问题、答案、知识连接、备注和标签搜索；
- 暂停的卡片不会进入复习队列；
- 删除卡片会同步清理复习状态与日志，并清除其他卡片指向它的失效前置关系。

### 3.3 预置体验知识库

首次启动且用户没有明确关闭默认内容时，系统自动建立：

- 科目：“操作系统”；
- 8 个章节；
- 160 张知识卡；
- 每张卡包含问题、答案、标签和重要程度。

初始化具有版本标记、中断修复和缺失自愈逻辑。默认内容会与用户知识并存，不覆盖用户自行创建的数据；设置页可手动恢复默认知识库。

### 3.4 知识路径

每张知识卡可以额外指定：

- 一个同科目的前置知识卡；
- 一段说明两张卡关系的“知识连接”文字。

当前路径能力包括：

- 防止知识卡关联自身；
- 防止跨科目前置关系；
- 防止形成循环依赖；
- 复习队列尽量将前置知识排在依赖知识之前；
- 学习当前卡片前，最多展示 3 个前置节点；
- 旧卡片没有显式前置关系时，以同章节中更早录入的卡片作为保守上下文；
- 删除前置卡片后保留依赖卡片，只清除断开的链接。

当前模型是“每张卡至多一个直接前置节点”，因此更接近知识链或树，不是支持多前置、多关系类型的完整知识图谱。

### 3.5 FSRS 复习调度

核心调度由 FSRS 完成，保存每张卡的完整 FSRS 状态和下次到期时间。

复习队列规则：

1. 只选择活跃卡片；
2. 支持科目、章节、未分类和标签过滤；
3. 已到期旧卡优先；
4. 旧卡按到期时间排序，同时考虑知识路径先后；
5. 新卡按创建时间和知识路径排序；
6. 新卡受每日新卡上限约束；
7. 当天已经首次学习的新卡会计入上限，重新进入页面不会重复补满。

用户可以调整：

- 每日新卡数量：0～200；
- 目标记忆保持率：75%～97%；
- 是否启用 FSRS 到期日期模糊分散。

### 3.6 复习交互

复习页根据卡片状态区分两类流程。

新卡学习：

1. 同一科目和章节的新卡按知识路径组成预览批次，每批最多 8 张；
2. 一次浏览本批问题和答案，也可以跳过预览；
3. 进入逐卡独立回忆阶段；
4. 显示答案；
5. 自我评分。只有首次评分才计入学习量，预览本身不改变进度。

已学卡复习：

1. 先显示问题；
2. 用户可以选择查看“脉络提示”；
3. 显示答案和备注；
4. 自我评分。

评分选项：

- 重来；
- 困难；
- 记住；
- 简单。

每个评分按钮会预览本次评分对应的下次复习间隔。评分后若 FSRS 状态仍处于学习或重学步骤，卡片会按实际到期时间回到本轮（包括跨过午夜的短间隔）；等待不会阻塞学习，用户可以继续学习后面的新知识。

其他交互：

- 显示当前进度和总卡数；
- 撤销上一次评分：恢复原 FSRS 状态并删除对应日志；
- 同一天、同一过滤范围内可恢复中断的复习；
- 等待或完成一批后可以继续学习 20 张新卡；
- 可以主动复习今日全部知识，或只复习今日曾选“重来 / 困难”的知识；
- 用户完成并返回后清除会话断点；
- 使用提示后提醒用户如实选择“重来”或“困难”，但目前不自动限制评分。

### 3.7 路径学习与专项筛选

用户可以组合选择：

- 全部或指定科目；
- 全部章节、指定章节或未分类；
- 全部或指定标签。

界面会提前显示本轮队列数量，并通过统一的路由序列化逻辑进入复习页。筛选复习仍遵守 FSRS 到期优先、每日新卡上限和前置知识顺序。

### 3.8 学习统计

目前统计内容包括：

- 今日复习次数；
- 今日首次学习卡片数；
- 今日“重来”次数；
- 连续学习天数；
- 最近 7 天复习次数柱状图；
- 容易遗忘的知识卡列表。

薄弱卡算法：

- 取每张卡最近 10 次评分；
- 重来记 3 分、困难记 1 分、记住记 0 分、简单记 -1 分；
- 至少有 3 次记录且总分大于 0 才进入薄弱列表；
- 按薄弱分和复习次数排序，最多显示 10 张；
- 可以从薄弱列表直接进入知识卡编辑页。

### 3.9 数据备份与恢复

- 导出全部科目、章节、卡片、FSRS 状态、复习日志和设置为 JSON；
- 自动复制导出内容到剪贴板；
- 导入前进行严格结构、类型和引用关系校验；
- 校验重复 ID、孤立章节、孤立卡片、跨科目前置关系、循环知识路径和损坏的 FSRS 状态；
- 导入前自动保存当前数据快照；
- 导入失败时自动回滚；
- 支持恢复最近一次导入前快照；
- 导入后清理旧复习会话，避免断点引用已被替换的数据。

## 4. 数据可靠性与隐私

### 4.1 数据位置

| 场景 | 数据位置 |
|---|---|
| H5 开发环境 | 项目目录下 `.recalllab-data/storage.json` |
| H5 正式部署 | 当前浏览器本地存储 |
| 微信小程序 | 微信原生本地存储 |

三个环境的数据默认互相独立，只能通过 JSON 备份手动迁移。

### 4.2 原子写入

评分、撤销、级联删除、预置初始化和备份导入涉及多个数据集合。项目提供批量事务能力：

- H5 开发 JSON 服务把多键变更合并为一次临时文件替换；
- 浏览器和微信端先保存待完成事务，再逐项写入；
- 如果应用在写入中途关闭，下次访问数据前会自动继续未完成事务；
- 评分状态、评分日志和复习会话断点在同一事务中提交。

### 4.3 隐私现状

- 不要求账号；
- 没有云端数据库；
- 没有广告或额外统计追踪；
- 没有向第三方发送学习内容；
- 局域网开发数据接口没有身份认证，只应在可信私人网络使用，不应暴露到公网。

## 5. 当前页面结构

底部主导航：

1. 首页；
2. 知识库；
3. 统计；
4. 设置。

二级页面：

- 科目详情；
- 知识卡编辑；
- 路径学习筛选；
- 沉浸式复习。

界面采用响应式卡片布局，支持手机安全区、H5 和微信小程序。复习页使用自定义导航，以减少学习过程中的干扰。

## 6. 代码结构

```text
src/
├── components/    可复用 UI 组件
├── data/          预置知识数据
├── pages/         8 个业务页面
├── scheduler/     FSRS 适配层
├── services/      卡片、科目、复习、统计、备份业务逻辑
├── storage/       跨平台存储与事务恢复
├── stores/        Pinia 页面状态和复习会话状态
├── types/         领域数据类型
└── utils/         日期、ID、复习路由等工具

server/
├── jsonStorage.ts       开发环境 JSON 文件存储
└── lanStoragePlugin.ts  Vite 开发存储接口
```

业务逻辑主要位于 service、scheduler 和 storage 层，页面主要负责展示与交互。

## 7. 当前质量保障

当前工作区：

- TypeScript 严格类型检查通过；
- 11 个测试文件；
- 47 项自动化测试全部通过。

测试覆盖的主要场景：

- FSRS 四种评分和参数归一化；
- 到期卡、新卡上限与知识路径排序；
- 新卡批次预览、跳过预览和会话恢复；
- 学习步骤按实际间隔重入本轮，等待时继续学习且不丢失待巩固卡；
- 今日知识与今日错题的主动复习队列；
- 过滤条件路由的生成和解析；
- 复习状态、日志、会话的联合提交；
- 撤销评分；
- 断点保存和清理；
- 知识路径上下文、旧数据回退和循环阻止；
- 卡片/科目删除及引用清理；
- 统计、连续学习和薄弱卡算法；
- 预置 160 张知识卡完整性、安全初始化与手动恢复；
- 备份格式、引用关系和 FSRS 状态校验；
- 导入前恢复点；
- H5 开发 JSON 存储；
- 生产 H5 不依赖开发存储接口；
- 本地事务中断后的自动恢复。

当前自动化测试以领域逻辑和存储逻辑为主，尚未覆盖完整页面端到端操作和微信开发者工具中的真机行为。

## 8. 已知限制与尚未实现功能

以下内容应视为明确的现状，不要把它们误认为已有能力：

1. 没有账号、云同步或自动跨设备同步；
2. 没有多端冲突合并机制；
3. 没有微信订阅消息、系统提醒或学习计划通知；
4. 不支持图片、音频、视频、公式、Markdown 或富文本卡片；
5. 不支持填空题、选择题、双向卡等多种卡片模板；
6. 不支持 CSV、Excel、Markdown 或 Anki 批量导入；
7. 知识路径只有单一前置节点，不是完整知识图谱；
8. “重要程度”目前主要用于展示，没有直接参与 FSRS 参数或队列优先级；
9. 统计只覆盖今日、7 天趋势和简单薄弱卡，没有长期保持率、未来负荷预测或科目对比；
10. 数据集合仍以完整数组读写，日志达到数万条后可能出现性能问题；
11. 备份版本为 v1，缺少完整的长期数据结构迁移框架；
12. 没有 PWA 离线安装、桌面客户端或原生 App；
13. 没有 UI 端到端测试、可访问性专项测试和持续集成流水线；
14. 没有正式的错误上报、性能监控和崩溃分析；
15. 局域网开发存储接口没有鉴权。

## 9. 希望其他模型重点评估的问题

请基于“个人、轻量、本地优先、H5 + 微信小程序”这个定位评估，不要默认项目必须变成大型云平台。

建议重点回答：

1. 哪些现有功能真正形成了产品差异化，哪些只是基础能力？
2. “FSRS + 前置知识路径”是否有学习科学上的合理性？哪些交互可能增加认知负担？
3. 新卡“小批次集中预览，再逐卡独立回忆”的流程是否自然？批次大小是否合理？
4. 知识路径应继续保持单父节点，还是升级为多前置知识图谱？
5. 下一阶段最值得实施的 5 项改进是什么？为什么？
6. 哪些建议能明显提升留存，但开发成本较低？
7. 哪些改进看起来高级，但不适合当前规模，应明确推迟？
8. 当前本地事务、备份和跨端数据策略还有哪些数据丢失风险？
9. 微信小程序端有哪些平台特有的体验或发布风险？
10. 当前统计指标能否帮助用户改善学习？还缺哪几个最关键指标？
11. 数量增长到 1 万张卡、10 万条复习日志时，架构需要怎样演进？
12. 测试和工程质量方面，下一轮最值得补充哪些场景？

## 10. 建议统一使用的评审输出格式

为了便于综合多个模型的答案，请要求每个模型按以下结构输出：

```text
一、总体评价
- 产品成熟度（1～10）
- 技术可靠性（1～10）
- 学习效果潜力（1～10）
- 微信小程序适配度（1～10）

二、现有优势
- 最多 5 项，每项说明证据和用户价值

三、主要问题
- 按严重程度排序
- 区分事实、推测和需要验证的假设

四、改进建议表
- 建议名称
- 用户价值
- 实施成本：低/中/高
- 技术风险：低/中/高
- 优先级：P0/P1/P2/P3
- 是否建议现在实施

五、最推荐的下一轮范围
- 最多选择 3～5 项
- 给出选择理由、依赖关系和验收标准

六、不建议当前实施的内容
- 说明推迟原因和适合重新评估的条件

七、需要产品负责人回答的问题
- 只列真正会影响方案选择的问题
```

## 11. 可直接复制给其他模型的提示词

```text
你是一名兼具学习科学、消费级产品设计、Vue/uni-app 工程和微信小程序经验的独立评审者。

请阅读随附的《苦作舟：功能、架构与改进评审说明》。不要假设文档没有写出的能力已经存在，也不要为了显得全面而机械建议社交、AI、云端或游戏化功能。

目标是帮助一款“个人、轻量、本地优先、FSRS + 前置知识路径、支持 H5 与微信小程序”的产品选择下一轮最值得实施的改进。

请：
1. 明确区分事实、推测和待验证假设；
2. 同时评估用户价值、学习效果、开发成本、数据风险和微信平台限制；
3. 优先推荐能形成完整闭环的改进，不要给零散功能清单；
4. 指出至少 3 项现在不应该做的内容；
5. 严格使用文档第 10 节规定的输出格式。
```
